const newOne = new Number(1)
const sympleOne = 1

console.log(newOne, sympleOne, newOne == sympleOne, newOne === sympleOne, Number(1))

const newStr = new String('1')
const sympleStr = '1'

console.log(newStr, sympleStr, newStr == sympleStr, newStr === sympleStr)

const newBool = new Boolean(true)
const sympleBool = true

console.log(newBool, sympleBool, newBool == sympleBool, newBool === sympleBool)

